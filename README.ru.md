# API Mocker Pro

Мощное расширение для Chrome, позволяющее имитировать API-запросы во время разработки и тестирования. Перехватывайте HTTP-запросы и отвечайте собственными данными без изменения кода приложения.

[English version](README.md)

## Возможности

### 🎯 Перехват запросов

- **Сопоставление по шаблону URL**: Используйте подстановочные знаки (`*`) или регулярные выражения
- **Фильтрация по HTTP-методу**: Имитируйте конкретные методы (GET, POST, PUT, DELETE, PATCH) или все сразу
- **Настраиваемые ответы**: Определяйте ответы в формате JSON, текста, HTML или XML
- **Управление кодом состояния**: Устанавливайте любой HTTP-код (200, 404, 500 и т.д.)
- **Задержка ответа**: Имитируйте задержку сети для тестирования состояний загрузки

### 🔄 Динамические переменные

Генерируйте динамические данные в ответах с помощью встроенных переменных:

- `{{timestamp}}` - Текущая временная метка Unix
- `{{uuid}}` - Случайный UUID v4
- `{{random_number}}` - Случайное число от 0 до 999999

Пример:

```json
{
  "id": "{{uuid}}",
  "timestamp": {{timestamp}},
  "order_number": {{random_number}}
}
```

### 📊 Журнал запросов

- **Запись для конкретной страницы**: Записывайте запросы с определённой вкладки браузера
- **Ручное управление**: Запускайте/останавливайте запись с помощью кнопок
- **Детали запросов**: Просматривайте URL, метод, код состояния и временную метку
- **Поиск и фильтрация**: Быстро находите запросы с помощью функции поиска
- **До 1000 запросов**: Автоматическая ротация журнала сохраняет последние запросы
- **Быстрое создание моков**: Создавайте правила имитации прямо из журнала с помощью кнопки "Mock This"

### 💡 Интерфейс

- **Плавающее окно**: Открывается в отдельном всплывающем окне для удобной многозадачности
- **Две вкладки**: "Mock Rules" для управления моками, "Requests Log" для просмотра перехваченных запросов
- **Переключатели**: Включайте/отключайте отдельные правила или глобальную имитацию
- **Визуальная обратная связь**: Чёткие индикаторы для включённых/отключённых правил и статуса записи
- **Поиск**: Мгновенная фильтрация правил и запросов
- **Адаптивный дизайн**: Чистый современный интерфейс с плавными анимациями

## Установка

### Из исходников

1. **Клонируйте или скачайте репозиторий**

   ```bash
   git clone <repository-url>
   cd api-mocker-extension
   ```

2. **Установите зависимости**

   ```bash
   npm install
   ```

3. **Соберите расширение**

   ```bash
   npm run build
   ```

   Это создаст папку `dist` со скомпилированным расширением.

4. **Загрузите в Chrome**
   - Откройте Chrome и перейдите на `chrome://extensions`
   - Включите "Режим разработчика" (переключатель в правом верхнем углу)
   - Нажмите "Загрузить распакованное расширение"
   - Выберите папку `dist`

### Режим разработки

Для разработки с автоматической пересборкой при изменениях:

```bash
npm run dev
```

## Быстрый старт

### Создание первого правила имитации

1. **Нажмите на иконку расширения**, чтобы открыть API Mocker
2. **Перейдите на вкладку "Mock Rules"**
3. **Нажмите "+ Add Rule"**
4. **Заполните форму**:
   - **Rule Name**: "Mock User API"
   - **URL Pattern**: `https://api.example.com/users/*`
   - **Match Type**: Wildcard
   - **Method**: GET
   - **Status Code**: 200
   - **Content Type**: application/json
   - **Response Body**:
     ```json
     {
       "id": "{{uuid}}",
       "name": "Тестовый пользователь",
       "email": "test@example.com"
     }
     ```
5. **Нажмите "Save Rule"**
6. **Включите правило** с помощью переключателя

Теперь все GET-запросы к `https://api.example.com/users/*` будут возвращать ваши данные!

### Запись и имитация запросов

1. **Откройте сайт, который хотите тестировать**
2. **Нажмите на иконку расширения**
3. **Перейдите на вкладку "Requests Log"**
4. **Нажмите "Start Recording This Page"**
5. **Взаимодействуйте с сайтом**, чтобы сгенерировать запросы
6. **Просмотрите записанные запросы** в списке
7. **Нажмите "Mock This"** на любом запросе, чтобы мгновенно создать правило

## Примеры использования

### Имитация API-эндпоинта

```
URL Pattern: https://api.github.com/users/*
Method: GET
Response:
{
  "login": "testuser",
  "id": {{random_number}},
  "created_at": "{{timestamp}}"
}
```

### Имитация ошибки

```
URL Pattern: https://api.example.com/checkout
Method: POST
Status Code: 500
Response:
{
  "error": "Сервис оплаты недоступен",
  "code": "SERVICE_ERROR"
}
```

### Имитация медленной сети

```
URL Pattern: https://api.example.com/data
Delay: 3000 (3 секунды)
Response: {"data": "медленный ответ"}
```

### Сопоставление через регулярное выражение

```
URL Pattern: https://api\.example\.com/(users|customers)/\d+
Match Type: Regex
Response: {"id": {{random_number}}, "type": "entity"}
```

## Сопоставление шаблонов URL

### Шаблон с подстановочными знаками

- `*` соответствует любым символам
- Примеры:
  - `https://api.example.com/*` - все запросы к этому домену
  - `https://*/users` - /users на любом домене
  - `*://api.example.com/v*/users` - любой протокол и версия

### Регулярное выражение

- Полная поддержка regex с синтаксисом JavaScript
- Не включайте начальные/конечные слэши
- Примеры:
  - `https://api\.example\.com/users/\d+` - соответствует /users/123
  - `https://.*\.example\.com/.*` - все поддомены

## Структура проекта

```
api-mocker-extension/
├── src/
│   ├── background.ts        # Service worker (перехват запросов)
│   ├── popup.ts             # Логика UI и обработчики событий
│   ├── storage.ts           # Обёртка Chrome Storage API
│   ├── types.ts             # Определения типов TypeScript
│   ├── utils.ts             # Утилиты
│   ├── ruleMatcher.ts       # Логика сопоставления шаблонов URL
│   └── responseGenerator.ts # Генерация динамических ответов
├── public/
│   ├── manifest.json        # Манифест расширения
│   ├── popup.html           # Структура UI
│   ├── popup.css            # Стили
│   └── icons/               # Иконки расширения
├── dist/                    # Результат сборки (генерируется)
├── webpack.config.js        # Конфигурация Webpack
├── tsconfig.json            # Конфигурация TypeScript
└── package.json             # Зависимости проекта
```

## Технические детали

### Архитектура

- **Manifest V3**: Использует новейшие API расширений Chrome
- **TypeScript**: Полностью типизированная кодовая база
- **Webpack**: Сборщик модулей для оптимизированных сборок
- **Service Worker**: Фоновый скрипт для перехвата запросов
- **declarativeNetRequest**: Chrome API для модификации сетевых запросов
- **webRequest API**: Для логирования и мониторинга запросов
- **Chrome Storage**: Постоянное хранилище данных для правил и журналов

### Разрешения

- `declarativeNetRequest`: Изменение сетевых запросов
- `webRequest`: Мониторинг сетевой активности
- `storage`: Сохранение правил и настроек
- `tabs`: Доступ к информации о вкладках для записи
- `contextMenus`: Интеграция контекстного меню
- `host_permissions`: Доступ ко всем URL для перехвата

### Хранение данных

Все данные хранятся локально с помощью `chrome.storage.local`:

- Правила имитации с полной конфигурацией
- Журналы запросов (до 1000 записей)
- Настройки и предпочтения
- Автосохранение черновиков несохранённых правил

## Разработка

### Сборка

```bash
npm run build      # Продакшн-сборка
npm run dev        # Сборка для разработки с отслеживанием изменений
npm run clean      # Удалить папку dist
```

### Структура кода

**background.ts** - Service Worker

- Инициализируется при установке/обновлении расширения
- Управляет правилами declarativeNetRequest
- Слушает события webRequest для логирования
- Обрабатывает сообщения от popup
- Управляет состоянием записи

**popup.ts** - Контроллер UI

- Управляет интерфейсом с двумя вкладками
- Обрабатывает взаимодействие с формами
- Опрос журнала запросов в реальном времени
- Автосохранение черновиков
- Логика поиска и фильтрации

**storage.ts** - Слой данных

- Обёртка Chrome Storage API
- Предоставляет типобезопасные операции хранения
- Управляет правилами, журналами и настройками
- Валидация данных

**responseGenerator.ts** - Движок имитации

- Преобразует правила в формат Chrome declarativeNetRequest
- Обрабатывает динамические переменные
- Генерирует ответы в base64
- Обрабатывает разные типы контента

**ruleMatcher.ts** - Сопоставление шаблонов

- Сопоставление с подстановочными знаками
- Сопоставление регулярных выражений
- Логика сравнения URL

## Решение проблем

### Расширение не работает

- Проверьте, включён ли переключатель "Mocking Enabled" (в заголовке)
- Убедитесь, что переключатели отдельных правил включены
- Проверьте, что шаблон URL соответствует вашему запросу
- Откройте консоль DevTools, чтобы увидеть логи расширения

### Запросы не записываются

- Убедитесь, что нажали "Start Recording This Page"
- Проверьте, что вы на правильной вкладке (запись привязана к вкладке)
- В статусе записи должен отображаться URL страницы
- Логируются только HTTP/HTTPS запросы

### Имитация не применяется

- Проверьте, что шаблон URL точно соответствует
- Проверьте совпадение HTTP-метода (или установите "Any")
- Убедитесь, что Content Type правильный
- Сначала протестируйте с шаблоном `*` для отладки

### Проблемы с производительностью

- Регулярно очищайте журнал запросов (1000+ записей могут замедлить UI)
- Отключайте неиспользуемые правила
- Используйте конкретные шаблоны URL вместо `*`, когда возможно

## FAQ

**В: Могу ли я имитировать запросы на любом сайте?**  
О: Да, расширение имеет host_permissions для всех URL.

**В: Синхронизируются ли мои правила между устройствами?**  
О: Нет, правила хранятся локально в Chrome storage. Функции экспорта/импорта будут добавлены позже.

**В: Можно ли имитировать WebSocket соединения?**  
О: Нет, в настоящее время поддерживаются только HTTP/HTTPS запросы.

**В: Работает ли это в режиме инкогнито?**  
О: Да, если включить "Разрешить в режиме инкогнито" в настройках расширения.

**В: Можно ли использовать для автоматизированного тестирования?**  
О: Это расширение предназначено для ручного тестирования. Для автотестов используйте инструменты типа MSW или Nock.

## Участие в разработке

Приветствуются любые дополнения! Пожалуйста, следуйте этим рекомендациям:

1. Сделайте форк репозитория
2. Создайте ветку для новой функции
3. Внесите изменения с понятными коммит-сообщениями
4. Тщательно протестируйте
5. Отправьте pull request

## Лицензия

MIT License - используйте это расширение свободно в своих проектах.

## Благодарности

Создано с использованием:

- TypeScript
- Chrome Extension Manifest V3
- Webpack
- Современный CSS с Flexbox

---

**Нужна помощь?** Создайте issue на GitHub или обратитесь к разделу решения проблем выше.
